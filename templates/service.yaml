apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: {{.namespace}}
  labels:
    app: kafka
spec:
  ports:
  - port: 9999
    name: tcp-jmx
  - port: 9093
    name: tcp-server
  clusterIP: None
  selector:
    app: kafka
